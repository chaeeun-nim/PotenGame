// import { useState } from 'react';

export default function SelectBtn() {
  // const [select, out] = useState(true);
  return (
    <>
      <button>선택</button>
      <button>선택해제</button>
    </>
  );
}
